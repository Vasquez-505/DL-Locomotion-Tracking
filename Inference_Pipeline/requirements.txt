# Inference Pipeline — Python dependencies
# Install with: pip install -r requirements.txt
#
# Requires Python 3.11–3.13
#
# ── torch install (choose ONE) ────────────────────────────────────────────
# CPU-only (machines without a compatible CUDA driver, e.g. driver < 520):
#   pip install torch==2.11.0+cpu torchvision==0.26.0+cpu --index-url https://download.pytorch.org/whl/cpu
#
# GPU / CUDA (machines with NVIDIA driver ≥ 520):
#   pip install torch==2.11.0 torchvision==0.26.0
#
# Then install the rest:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────

sleap-nn==0.1.3
sleap-io==0.6.5
h5py==3.16.0
pandas==3.0.2
matplotlib==3.10.8
psutil==7.0.0
PyYAML==6.0.2
opencv-python==4.13.0.92
scipy==1.17.1
